# 发布规则

- 每项功能和修复必须先合入 `main`。不得从独立功能分支直接发布正式版本。
- 发布前更新本地 `origin/main`，确认所需功能已包含在同一个 `main` 提交中，再确定统一版本号并创建标签。
- Android、桌面端、CLI、服务端及正式 Docker 镜像必须通过统一 Release 工作流，从同一个标签提交构建。各端版本号保持一致。
- 不得把其他提交构建的安装包补充或覆盖到已有正式 Release，也不得移动已发布标签。缺少功能或产物时，修复合入 `main` 后统一发布下一个版本。
- Android 独立工作流仅用于测试构建，不作为正式发布途径。正式签名通过统一 Release 工作流使用既有密钥。
- 发布前确认检查通过及各端产物齐全；区分“代码合入”“GitHub 发布”“线上部署/安装包同步”，如实报告各阶段状态。
